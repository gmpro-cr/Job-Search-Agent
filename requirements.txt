# Slim runtime for the Vercel serverless deploy.
# Everything heavy (selenium, langgraph, googleapiclient, ollama, ...) is
# imported lazily inside scraper / agent code paths and lives in
# requirements-scraper.txt. GitHub Actions installs BOTH files.

# Web framework
flask>=3.0.0
authlib>=1.3.0
python-dotenv>=1.0.0

# DB / storage
psycopg[binary]>=3.2

# HTTP / parsing used at request time (Telegram, gap analysis, etc.)
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0

# CV upload parsing
pdfplumber>=0.10.0
python-docx>=1.1.0
